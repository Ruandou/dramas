# 短剧制片规范模板（通用）

> **版本**：v1.0 · 2026-05-29
> **用途**：复制到 `dramas/<剧名>/制片规范.md`，按 `{占位符}` 填入剧名相关内容

---

## 一、ID 命名规则

| 类型     | 格式                              | 示例              | 用途                             |
| -------- | --------------------------------- | ----------------- | -------------------------------- |
| **角色** | `CHAR-###`                        | `CHAR-001`        | 对白、关系、选角（谁）           |
| **形象** | `CHAR-###-L##`                    | `CHAR-001-L01`    | 出图、妆造、分镜视觉（长什么样） |
| 群演     | `CHAR-GRP-##` / `CHAR-GRP-##-L01` | `CHAR-GRP-01-L01` | 通用脸型一套即可                 |
| 场景     | `SCENE-###`                       | `SCENE-002`       | 布景                             |
| 分集     | `EP##`                            | `EP01`            | 分集文件                         |
| 道具     | `PROP-###`                        | `PROP-001`        | 关键道具                         |

- 编号**只增不删**；弃用标 `deprecated: true`。
- 新建角色：先 `资产/角色索引.md`，再建 **`L01` 基础形象**于 `资产/形象索引.md` + `角色卡.md`。
- 新建换装/阶段：**只加 `L02+` 衍生形象**，并写明 `based_on: CHAR-xxx-L01`。

### 1.1 形象 ID 与基础 / 衍生

| 后缀     | 名称     | 要求                                                                                |
| -------- | -------- | ----------------------------------------------------------------------------------- |
| **L01**  | 基础形象 | 完整定妆 Prompt（脸、体型、发式、默认服装）；全剧面部一致性的**唯一锚点**           |
| **L02+** | 衍生形象 | **必须** `based_on` 本角色 `L01`；Prompt 只写差异，并含 `same face as CHAR-xxx-L01` |

**出图流程**：定稿 `L01` 参考图 → 衍生形象用图生图 / 角色一致性模型 → 禁止跳过 L01 直接生成 L02。

**本剧衍生形象表**（按需填写）：

| 角色    | L01（基础）    | L02+（衍生示例）         |
| ------- | -------------- | ------------------------ |
| {角色A} | {基础服装描述} | {衍生1描述}、{衍生2描述} |
| {角色B} | {基础服装描述} | {衍生1描述}              |

---

## 二、目录结构

```
{剧名}/
├── 资产/
│   ├── 角色索引.md          # CHAR-* 主表（角色卡详情可链入）
│   ├── 场景卡片.md          # SCENE-* 主表
│   └── {年代/美术规范}.md   # 如有特殊时代/视觉约束（可选）
├── 剧本/
│   ├── README.md
│   ├── _模板.md
│   ├── EP01_{集标题}.md
│   └── …
├── assets/
│   ├── generated/           # AI 生成成品（禁止写入占位）
│   │   └── EP##/
│   ├── looks/               # 角色定妆参考图
│   ├── scenes/              # 场景参考图
│   ├── props/               # 道具参考图
│   └── voices/              # 音色参考
├── docs/
│   └── {专项文档}.md        # 如：术语表、年代规范、流水线文档等
├── script/                  # 自动化脚本
├── 角色卡.md                # 外貌/Prompt 详情（含 CHAR ID）
├── 制片规范.md              # 本文件
├── 短剧剧本_{剧名}_36集.md  # 主剧本纲要
└── 人物小传_{剧名}.md       # 可选
```

**约定**：

- **纲要**在 `短剧剧本_{剧名}_36集.md` 维护剧情。
- **可拍分镜 + 对白**一律在 `剧本/EP##_*.md`，并带 CHAR/SCENE 引用。

---

## 三、工作流顺序（必读 · 防不同步）

### 3.1 修改顺序

改台词、场次、字幕/旁白策略时**严格按层向下**，**禁止**先改 `segments.yaml` 或 `shots.yaml` 再回头补剧本。

| 顺序  | 层           | 文件                        | 写什么                                                                          |
| :---: | ------------ | --------------------------- | ------------------------------------------------------------------------------- |
| **1** | **主剧本**   | `短剧剧本_{剧名}_36集.md`   | 集纲、每场剧情、对白要点、钩子                                                  |
| **2** | **分集剧本** | `剧本/EP##_*.md`            | 可拍分镜表：`shot_id`、画面、**对白/备注**、CHAR/SCENE/形象、时长/模式/运镜     |
| **3** | **机器可读** | 同目录 `EP##_shots.yaml`    | 从分集 md 导出，勿手改 yaml 当源                                                |
| **4** | **段落 API** | 同目录 `EP##_segments.yaml` | 按 md 镜号合并为 segment；`api.text` 对白/字幕/旁白与 **第 2 步引号内字面一致** |
| **5** | **其它**     | 声音卡、年代规范、assets/   | 仅在前几步定稿后随需更新                                                        |

**Agent / 人工自检**（改完第 2 步后）：

- [ ] 主剧本该集对白要点已与分集表一致（或注明「分集为准」并只改分集）
- [ ] 未单独改 `segments` 而 md 未动

**单集真相源**：拍什么、说什么 → **`剧本/EP##_*.md`**；主剧本为纲，segments 为 API 展开，**不是**编剧起点。

---

## 四、分集文件头（YAML）

每集文件顶部：

```yaml
---
episode_id: EP01
episode_title: { 集标题 }
duration_min: { 2-3 }
season: S1
scene_ids: [SCENE-001, SCENE-002]
character_ids: [CHAR-001, CHAR-002]
look_ids: [CHAR-001-L01, CHAR-002-L01]
seedance_defaults:
  model: seedance-2.0
  ratio: "9:16"
  resolution: 720p
  duration_sec: 10-12
  generate_audio: true
---
```

**字段说明**：

| 字段                | 说明                            |
| ------------------- | ------------------------------- |
| `episode_id`        | `EP##` 格式                     |
| `episode_title`     | 集标题                          |
| `duration_min`      | 目标时长（分钟），一般 2–3 分钟 |
| `season`            | 固定 `S1`（全 36 集为第一季）   |
| `scene_ids`         | 本集涉及场景 ID 列表            |
| `character_ids`     | 本集涉及角色 ID 列表            |
| `look_ids`          | 本集涉及形象 ID 列表            |
| `seedance_defaults` | Seedance 生成默认参数           |

---

## 五、时代/视觉约束（按需填写）

> 如果是**现代剧**，本节可简化为 Negative Prompt 模板。
> 如果是**年代剧/古装剧**，需详细列出时代特征和禁止元素。

### 5.1 时代特征（如有）

| 类别         | 准确元素                                                   |
| ------------ | ---------------------------------------------------------- |
| **通讯设备** | {如：智能手机、笔记本电脑——或年代特定：BP机、座机}         |
| **家电**     | {如：液晶电视、智能音箱——或年代特定：CRT电视、VCD机}       |
| **交通**     | {如：现代轿车——或年代特定：桑塔纳、自行车}                 |
| **建筑**     | {如：现代写字楼、玻璃幕墙——或年代特定：筒子楼、水泥砖墙}   |
| **服饰**     | {如：现代商务休闲——或年代特定：夹克衫、中山装、涤纶衬衫}   |
| **商业环境** | {如：连锁商场、便利店——或年代特定：个体户铁皮棚、国营百货} |

### 5.2 禁止出现（Negative Prompt 必加）

| 类别         | 禁止元素                  |
| ------------ | ------------------------- |
| **通讯**     | {如需排除的通讯设备}      |
| **家电**     | {如需排除的家电}          |
| **交通**     | {如需排除的车辆/交通方式} |
| **建筑**     | {如需排除的建筑风格}      |
| **服饰**     | {如需排除的服饰品牌/风格} |
| **道具**     | {如需排除的现代道具}      |
| **场景氛围** | {如需排除的灯光/装修风格} |

### 5.3 Negative Prompt 模板

```
{按需填写，现代剧一般排除年代元素，年代剧排除现代元素}
例如：
smartphone, flat screen TV, LED lights, modern car, laptop, tablet,
QR code, delivery box, power bank, glass curtain wall, subway station,
LED billboard, wireless earbuds, ring light, bubble tea cup,
modern minimalist interior, neon RGB lighting
```

---

## 六、AI 出图/视频衔接

### 6.1 出图流程

1. 分镜 **`场景`** → `资产/场景卡片.md` → `SCENE-###`
2. 分镜 **`形象`** → `资产/形象索引.md` → `角色卡.md` 内该 **L##** 的 Prompt
3. **L01**：用「基础 Prompt」生成 Character Sheet（正面全身，白底）
4. **L02+**：`based_on` 指向 L01 参考图 + **delta Prompt**（仅服装/道具/妆效/伤痕）
5. 多角色同框：按形象 ID 顺序合并；每人脸对应其 `L##`，禁止混用别角 L01
6. 分镜 **`角色`** 列仅用于对白表，不参与出图 Prompt 拼接

### 6.2 角色一致性规则

- 每个角色 **L01** 生成后，所有后续出现必须使用该 L01 作为 reference_image
- 衍生形象仅改变服装/状态，面部特征锁定为 L01
- 同集多角色：每人独立 L01 → 拼合时按角色顺序排列参考图

### 6.3 场景一致性规则

- 每个场景 **SCENE-###** 生成参考图后，后续同场景必须使用该参考图
- 同场景不同角度：用同一参考图做 img2img 变体，禁止重新生成

---

## 七、声音规范

### 7.1 TTS / 配音

| 项目     | 说明                                                   |
| -------- | ------------------------------------------------------ |
| 语言     | {中文/方言等}                                          |
| TTS 引擎 | {Edge TTS / Fish Audio / 其他}                         |
| 语速     | {如：正常偏快，短剧节奏}                               |
| 情绪标记 | 分集剧本中标注 `[怒]` `[笑]` `[低]` 等，TTS 按标记调整 |
| BGM      | {如：悬疑用弦乐，打脸用鼓点，温情用钢琴}               |

### 7.2 音效

| 场景类型 | 必备音效                                   |
| -------- | ------------------------------------------ |
| {场景1}  | {如：夜市——油烟声、叫卖声、碗筷碰撞}       |
| {场景2}  | {如：办公室——键盘声、电话铃、翻页声}       |
| {场景3}  | {如：对质场景——沉默时的环境白噪音、心跳声} |

---

## 八、投流/发布备忘

### 8.1 投流标题库

（从主剧本摘取，每集至少 3 条）

### 8.2 切片规则

- 每集至少 1 个 15 秒切片（集末钩子前 15 秒）
- 切片标题 = 投流标题库中的文案
- 封面图 = 该集打脸/反转瞬间的截图

### 8.3 发布节奏

- {如：日更 1 集 / 首日 3 集 + 日更 1 集 / 其他}
