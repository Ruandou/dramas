# 《全公司都以为我很有背景》制片规范 · ID 与引用

> **正名**：《全公司都以为我很有背景》  
> **版本**：v1.0 · 2026-05-27

---

## 一、ID 命名规则

| 类型     | 格式                              | 示例              | 用途                             |
| -------- | --------------------------------- | ----------------- | -------------------------------- |
| **角色** | `CHAR-###`                        | `CHAR-001`        | 对白、关系、选角（谁）           |
| **形象** | `CHAR-###-L##`                    | `CHAR-001-L01`    | 出图、妆造、分镜视觉（长什么样） |
| 群演     | `CHAR-GRP-##` / `CHAR-GRP-##-L01` | `CHAR-GRP-01-L01` | 通用脸型一套即可                 |
| 场景     | `SCENE-###`                       | `SCENE-002`       | 布景                             |
| 分集     | `EP##`                            | `EP01`            | 分集文件                         |
| 道具     | `PROP-###`                        | `PROP-001`        | 工牌、合同、手机等               |

- 编号**只增不删**；弃用标 `deprecated: true`。
- 新建角色：先 `资产/角色索引.md`，再建 **`L01` 基础形象**于 `资产/形象索引.md` + `角色卡.md`。
- 新建换装/阶段：**只加 `L02+` 衍生形象**，并写明 `based_on: CHAR-xxx-L01`。

### 1.1 形象 ID 与基础 / 衍生

| 后缀     | 名称     | 要求                                                                                |
| -------- | -------- | ----------------------------------------------------------------------------------- |
| **L01**  | 基础形象 | 完整定妆 Prompt（脸、体型、发式、默认服装）；全剧面部一致性的**唯一锚点**           |
| **L02+** | 衍生形象 | **必须** `based_on` 本角色 `L01`；Prompt 只写差异，并含 `same face as CHAR-xxx-L01` |

**出图流程**：定稿 `L01` 参考图 → 衍生形象用图生图 / 角色一致性模型 → 禁止跳过 L01 直接生成 L02。

**本剧常见衍生场景**：

| 角色   | L01（基础）          | L02+（衍生示例）                    |
| ------ | -------------------- | ----------------------------------- |
| 林舟   | 日常实习服装         | L02 高层工位正装、L03 对质场合西装  |
| 苏曼   | HR 专业装            | L02 便装（茶水间夜谈）              |
| 周建邦 | 裁剪套装（日常）     | L02 家宴休闲装                      |

---

## 二、目录结构

```
全公司都以为我很有背景/
├── 资产/
│   ├── 角色索引.md      # CHAR-* 主表（角色卡详情可链入）
│   └── 场景卡片.md      # SCENE-* 主表
├── 剧本/
│   ├── EP01/
│   │   └── EP01_电梯.md
│   ├── EP02/
│   │   └── EP02_窗位.md
│   └── EP03/
│       └── EP03_别澄清.md
├── assets/
│   ├── generated/       # AI 生成成品（禁止写入占位）
│   │   └── EP##/
│   ├── looks/           # 角色定妆参考图
│   ├── scenes/          # 场景参考图
│   ├── props/           # 道具参考图
│   └── voices/          # 音色参考
├── docs/
│   └── Seedance2_有声段落流水线.md
├── script/              # 自动化脚本
├── 角色卡.md            # 外貌/Prompt 详情（含 CHAR ID）
├── 制片规范.md          # 本文件
├── 工作计划.md
└── 短剧剧本_全公司都以为我很有背景_36集.md
```

**约定**：

- **纲要**仍可在 `短剧剧本_全公司都以为我很有背景_36集.md` 维护剧情。
- **可拍分镜 + 对白**一律在 `剧本/EP##/EP##_*.md`，并带 CHAR/SCENE 引用。

---

## 三、工作流顺序（必读 · 防不同步）

### 3.1 修改顺序

改台词、场次、字幕/旁白策略时**严格按层向下**，**禁止**先改 `segments.yaml` 或 `shots.yaml` 再回头补剧本。

| 顺序  | 层           | 文件                                                                   | 写什么                                                                               |
| :---: | ------------ | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| **1** | **主剧本**   | [`短剧剧本_全公司都以为我很有背景_36集.md`](./短剧剧本_全公司都以为我很有背景_36集.md) | 集纲、每场剧情、对白要点、钩子                                                       |
| **2** | **分集剧本** | [`剧本/EP##/EP##_*.md`](./剧本/)                                       | 可拍分镜表：`shot_id`、画面、**对白/备注**、CHAR/SCENE/形象、时长/模式/运镜          |
| **3** | **机器可读** | 同目录 `EP##_shots.yaml`                                               | 从分集 md 导出，勿手改 yaml 当源                                                     |
| **4** | **段落 API** | 同目录 `EP##_segments.yaml`                                            | 按 md 镜号合并为 segment；`api.text` 对白/字幕/旁白与 **第 2 步引号内字面一致**      |
| **5** | **其它**     | 声音卡、现代视觉规范、assets/                                           | 仅在前几步定稿后随需更新                                                             |

**Agent / 人工自检**（改完第 2 步后）：

- [ ] 主剧本该集对白要点已与分集表一致（或注明「分集为准」并只改分集）
- [ ] 未单独改 `segments` 而 md 未动

**单集真相源**：拍什么、说什么 → **`剧本/EP##/EP##_*.md`**；主剧本为纲，segments 为 API 展开，**不是**编剧起点。

---

## 四、分集文件头（YAML）

每集文件顶部：

```yaml
---
episode_id: EP01
episode_title: 电梯
duration_min: 2.5
season: S1
scene_ids: [SCENE-001, SCENE-002, SCENE-003]
character_ids: [CHAR-001, CHAR-002, CHAR-GRP-01]
look_ids: [CHAR-001-L01, CHAR-002-L01]
seedance_defaults:
  model: doubao-seedance-2-0-fast-260128
  ratio: "9:16"
  resolution: 1080x1920
  duration_sec: 8-10
  generate_audio: true
  prompt_suffix: "禁止画面中出现任何文字或字幕"
  negative_prompt: "real brand logos, real platform UI visible on screens, news headline text, real celebrity faces, watermark, historical or period costumes"
---
```

**字段说明**：

| 字段                | 说明                                    |
| ------------------- | --------------------------------------- |
| `episode_id`        | `EP##` 格式                             |
| `episode_title`     | 集标题                                  |
| `duration_min`      | 目标时长（分钟），本剧 2–3 分钟         |
| `season`            | 固定 `S1`（全 36 集为第一季）           |
| `scene_ids`         | 本集涉及场景 ID 列表                    |
| `character_ids`     | 本集涉及角色 ID 列表                    |
| `look_ids`          | 本集涉及形象 ID 列表                    |
| `seedance_defaults` | Seedance 生成默认参数                   |

---

## 五、现代都市视觉规范

### 5.1 必须体现（现代元素）

| 类别         | 准确元素                                                                         |
| ------------ | -------------------------------------------------------------------------------- |
| **办公空间** | 开放式工位区、玻璃隔断、落地幕墙、景观工位（高层河景）、会议室白板/投影幕      |
| **设备**     | 笔记本电脑、双屏工位、无线鼠标键盘、企业级打印机、考勤门禁刷卡机               |
| **通讯**     | 智能手机（背面/侧面不显示界面内容）、工位座机内线电话、工牌挂绳                 |
| **服装**     | 现代商务正装：衬衫、西装裤/裙、连衣裙、小跟鞋、平底鞋、西装外套               |
| **餐饮**     | 企业咖啡机（不显示品牌 logo）、饮水机、餐厅外卖袋（不显示平台名）             |
| **交通**     | 现代黑色商务轿车、地铁站（不显示运营商字幕）                                   |
| **楼宇**     | 高层玻璃幕墙写字楼、电动遮光帘、感应式旋转门、大堂接待台、安保闸机             |

### 5.2 禁止出现（Negative Prompt 必加）

| 类别           | 禁止元素                                                                                |
| -------------- | --------------------------------------------------------------------------------------- |
| **品牌 Logo**  | Apple/Samsung/Huawei logo、Nike/Adidas logo、任何可识别品牌标识                         |
| **屏幕内容**   | 微信/支付宝/淘宝/抖音界面、任何社交媒体 UI、新闻标题文字（含手机/电脑屏幕）           |
| **人物**       | 真实名人面孔、真实政治人物、可辨认的公众人物                                           |
| **年代穿帮**   | 古装/民国/八九十年代服饰，传呼机、CRT 电视、绿皮火车等历史道具                        |
| **水印/字幕**  | 任何画面内嵌字幕、制作方水印、平台角标                                                 |

### 5.3 Negative Prompt 模板

```
real brand logos, Apple logo, Samsung logo, Nike logo, Adidas logo,
real platform UI visible on screens, WeChat interface, Alipay interface,
Taobao interface, TikTok UI, news headline text,
real celebrity faces, real politician faces,
watermark, subtitles burned into frame,
historical costumes, period costumes, ancient Chinese clothing,
1980s fashion, 1990s fashion, Mao-style suit,
pager, beeper, CRT television, rotary telephone
```

### 5.4 场景视觉参考

| 场景类型     | 正确元素                                                    |
| ------------ | ----------------------------------------------------------- |
| 大堂电梯间   | 大理石地面、不锈钢电梯门、数字楼层显示、安保门禁感应闸      |
| 开放工位区   | 白色工位桌、双屏显示器、低矮玻璃隔断、悬挂植物             |
| 高层办公室   | 落地玻璃、城市/河流全景、深色木桌、单只兰花、极简装饰       |
| 茶水间       | 不锈钢台面、咖啡机、饮水机、圆桌椅、玻璃墙                 |
| 高档包间     | 深色木板墙、圆桌转盘、暖光吊灯、红酒冰桶、水墨画挂轴       |

---

## 六、AI 出图/视频衔接

### 6.1 出图流程

1. 分镜 **`场景`** → `资产/场景卡片.md` → `SCENE-###`
2. 分镜 **`形象`** → `资产/形象索引.md` → `角色卡.md` 内该 **L##** 的 Prompt
3. **L01**：用「基础 Prompt」生成 Character Sheet（正面全身，白底）
4. **L02+**：`based_on` 指向 L01 参考图 + **delta Prompt**（仅服装/场合/妆效）
5. 多角色同框：按形象 ID 顺序合并；每人脸对应其 `L##`，禁止混用别角 L01
6. 分镜 **`角色`** 列仅用于对白表，不参与出图 Prompt 拼接

### 6.2 角色一致性规则

- 每个角色 **L01** 生成后，所有后续出现必须使用该 L01 作为 reference_image
- 衍生形象仅改变服装/状态，面部特征锁定为 L01
- 多人同框时，每人必须挂对应的 `looks/CHAR-*-L##.png`

### 6.3 现代都市合规 Negative Prompt

所有生成任务（图片/视频）**必须**在 negative prompt 中包含 §5.3 模板内容，确保无品牌穿帮与年代穿帮。

### 6.4 场景出图要求

- 每个 `SCENE-*` 必须有对应参考图 `assets/scenes/SCENE-*.png`
- 场景参考图必须符合 §5.1 现代元素
- 注意：室内场景避免出现可识别品牌；屏幕若入画须做模糊处理

### 6.5 引用图片提交策略（TOS 优先）

Seedance 2.0 API 文档标称支持每段最多 9 张引用图片，但使用 base64 data URI 时，4+ 张高分辨率图片（2K PNG，单张 200-500KB）会因请求体积过大导致提交失败。

**策略：TOS 优先，base64 兜底**

**两级引用策略（优先级从高到低）**：

| 优先级 | 方式 | 有效期 | 适用场景 |
|--------|------|--------|----------|
| 1 | TOS URL | 永久 | 已上传至 TOS 的图片，请求体积最小 |
| 2 | base64 data URI | 永久 | TOS 未上传时的兜底方案，≤3 张/段 |

**TOS 永久图床**：
- 桶名/区域默认：`drama-reference-images` / `cn-beijing`
- URL 格式：`https://{bucket}.tos-cn-beijing.volces.com/{prefix}/{filename}`
- 上传工具：`mcps/volc-ark/scripts/tos_upload.py sync --project-root .`
- 上传后在 `cdn_urls.json` 的 `tos_url` 字段存储永久链接

**实施要求**：
- 上传图片至 TOS 后，在 `assets/looks/` 或 `assets/scenes/` 同目录下维护 `cdn_urls.json`：
  ```json
  {
    "CHAR-001-L01": {
      "local": "CHAR-001-L01.png",
      "tos_url": "https://drama-reference-images.tos-cn-beijing.volces.com/looks/CHAR-001-L01.png"
    }
  }
  ```
- 提交脚本优先读取 `tos_url`；若无则读取本地文件转 base64
- **TOS 模式下**：允许 4-6 张引用图片/段（多角色场景不再受限）
- **base64 模式下**：保守限制 ≤3 张引用图片/段
- 本地 PNG 文件始终保留作为最终备份

---

## 七、道具转移与肢体动作（防错清单）

适用：递交文件、握手、争抢合同、掀桌、推门等 **「谁碰什么」** 明确的镜。

### 7.1 常见模型错误

| 现象           | 原因                                                                       |
| -------------- | -------------------------------------------------------------------------- |
| 道具尺度漂移   | 手机忽大忽小；文件夹形状变化；prompt 写「文件」无具体描述                 |
| 配角先动手     | 上一镜主角对白，下一镜「递交/夺回/掷桌」未绑 **图N 主角**                  |
| 段内跳切不连贯 | prompt 写成镜头清单，各镜各画，不保证同一件道具、同一人持有               |
| 表情与台词不符 | 画面描述与对白情绪未对齐（如台词是愤怒，画面描述却是「微笑」）            |

### 7.2 分段原则（先于 prompt）

- **同一场戏、同一道具动作链** → **一条 segment**，勿拆成多条 API 任务。
- 对白镜与动作镜在同一 segment 时：**对白者 ≠ 动作者** 必须写清绑定。
- 单段 **≤1 句对白**；复杂情绪段尽量少人同框。

### 7.3 `api.text` 写法（强制）

1. **段首「角色分工」**：谁可接触道具、谁**禁止**（例：仅图1 林舟拿合同；图2 陈竞双手不得拿）。
2. **每镜绑 `图N` + 服装色**：用 `图1`/`图2`，不用泛称「男主」「同事」；`content_roles` 顺序与全文一致。
3. **道具锁定**：具体描述（白色 A4 合同文件夹，标准大小）；禁止堆叠模糊修饰词。
4. **因果链叙事**，非 Excel 清单：  
   `图2 走来递文件 → 图1 接过翻看 → 图1 抬眼质问 → 图2 后退一步`
5. **关键镜重复约束**：递交、夺回镜再写一次「图1 持文件 / 图2 空手」。

### 7.4 参考图

| 资产 | 路径                          | 用途               |
| ---- | ----------------------------- | ------------------ |
| 定妆 | `assets/looks/CHAR-*-L01.png` | 脸与服装           |
| 场景 | `assets/scenes/SCENE-*.png`   | 空间               |
| 道具 | `assets/props/PROP-*-*.png`   | 尺度与造型锚点     |

### 7.5 提交前检查

- [ ] 段首是否有 **角色分工 + 对手禁止碰道具**？
- [ ] 每句「递交/夺回/掷出」是否带 **图N**？
- [ ] 道具动作链是否 **未拆段**？`duration_sec` 是否 4–12？
- [ ] `content_roles` 图1 是否为 **剧本上的动作者**？
- [ ] Negative prompt 是否包含 §5.3 现代都市禁止项？

### 7.6 `segments.yaml` 模板片段（文件递交类）

```yaml
api:
  text: |
    【图1】林舟·男主（白色牛津衬衫，深色修身裤）【图2】陈竞（光亮深色西装，花纹领带）【图3】SCENE-004
    角色分工：仅图1可持文件夹；图2禁止触碰任何文件。
    道具：白色 A4 文件夹，标准大小，封面无文字。
    图2 走来将文件夹递到图1 桌上；图1 翻开第一页慢慢扫视。
    图1 抬头直视图2；图2 后退半步，笑容僵在脸上。
    图1 合上文件夹推回桌上，轻声开口。
content_roles:
  - { file: CHAR-001-L01, role: reference_image, label: 图1 }
  - { file: CHAR-004-L01, role: reference_image, label: 图2 }
  - { file: SCENE-004, role: reference_image, label: 图3 }
```

---

## 八、有声段落（Seedance 2.0 · 默认）

1. **提交单位**：`EP##_segments.yaml` 中每条 `segment_id`（约 8–12 秒），非一镜一任务。
2. **音频**：`generate_audio: true`；对白与 **voice_prompt** 写在 `api.text`。
3. **画面**：段落仅用 `looks/` + `scenes/`；不依赖镜级首帧。
4. **一段一落音**：单段主说话人 ≤2；长对手戏拆成 `SEGxxa` / `SEGxxb`。
5. **音色文案**：同一角色跨段 **voice_prompt 全文一致**。  
   - 林舟：「成年男性，23岁，语调平稳偏内敛，带有轻微茫然感，语速中等」  
   - 苏曼：「成年女性，27岁，冷静专业，语速偏快，克制而精准」  
   - 周建邦：「成年男性，58岁，声线低沉有力，语速从容，不疾不徐，带有掌控感」  
   5.1 **字幕（默认全段）**：后期通过 ffmpeg 统一添加，Seedance 不烧录字幕。  
   5.2 **字卡镜**：并入相邻段，画面字幕一字不差，无旁白念稿。
6. **验收**：全段耳听+目视字幕是否与引号一致。
7. **成片**：`assets/generated/EP##/` 下 `EP##-SEG*.mp4`；集级 `script/ffmpeg_concat_episode.sh`。
8. **段落时长（API）**：`duration_sec` 对 **fast** 只能 **4–12**（硬限制）。
9. **合理分段（制片）**：
   - **一条 API = 一段可拍的戏（约 4–12 秒信息密度）**，不是「一镜一条」。
   - 分镜表里某镜自然时长 **< 4 秒**：**并入同场景相邻段**，不要单独开段。
   - 拆段边界：场切换、主说话人切换、情绪断点；不要按 `shot_id` 机械切。

### 8.10 Seedance 段落 Prompt 结构规范

为避免双字幕和字幕不一致，每段 `api.text` 必须遵循以下结构：

```
【图1】角色 ID【图2】场景 ID。
竖屏9比16连贯叙事。
镜头1（Xs）[景别][运镜]：[纯视觉动作描述，不含对白]
镜头2（Xs）[景别][运镜]：[纯视觉动作描述，不含对白]
[以下对白仅供语音合成，严禁在画面中显示任何文字]
对白（角色A，年龄，情绪）：「台词」
对白（角色B，年龄，情绪）：「台词」
画面全程无任何文字、字幕、标题、水印。
现代中国都市写字楼，写实都市剧风格，竖屏9比16，无品牌 Logo，无平台 UI，无历史道具。
[negative_prompt]
[prompt_suffix]
```

**禁止事项：**
1. **禁止**在镜头描述中嵌入对白文字（会导致双字幕）
2. **禁止**在段落级添加 `字幕：...` 指令（全局 `prompt_suffix` 已包含统一字幕规则）
3. **禁止**在多处重复出现相同对白文字

**字幕统一由 `prompt_suffix` 控制：**
`"禁止画面中出现任何文字或字幕"`

### §8.11 字幕策略：后期添加

字幕**不由 Seedance 烧录**，改为后期通过 ffmpeg 统一添加。

- `prompt_suffix`: `"禁止画面中出现任何文字或字幕"`
- `prompt_suffix_silent`: `"本段无对白无语音，禁止画面中出现任何文字"`
- 对白文本保留在 prompt 中用于语音合成（`generate_audio: true`）
- 字幕样式（字体、字号、颜色、位置）在后期统一控制

#### §8.11.2 后期字幕流水线（Whisper + 校对 + 烧录）

整集拼接完成后，字幕**必须听写成片音频**，禁止用 `segments.yaml` 对白按时长比例估时。

**三步（Agent 须自动执行，含第 2 步校对）：**

```bash
cd dramas/全公司都以为我很有背景

# 1. 听写
HF_ENDPOINT=https://hf-mirror.com python3 script/gen_audio_srt.py \
  assets/generated/EP##/EP##_full.mp4 \
  -o assets/generated/EP##/EP##_audio.srt

# 2. 校对（对照 segments.yaml dialogue_lines，修同音错字；保留 Whisper 时间轴）
python3 script/review_audio_srt.py assets/generated/EP##/EP##_audio.srt

# 3. 烧硬字幕（Pillow + ffmpeg overlay，样式：白字描边、无黑底条）
python3 script/burn_subs_pillow.py \
  assets/generated/EP##/EP##_full.mp4 \
  assets/generated/EP##/EP##_audio.srt \
  -o assets/generated/EP##/EP##_subtitled.mp4
```

**Agent 校对要求（第 2 步后主动执行）：**

- 对照 `剧本/EP##/EP##_segments.yaml` 的 `dialogue_lines`
- **只修明显问题**：同音错字、专有名词（林舟、陈竞、苏曼等）
- **禁止**把短听写句整段替换成剧本长句（会破坏口型/时长）

---

## 九、修订记录

| 版本 | 日期       | 说明                                                                           |
| ---- | ---------- | ------------------------------------------------------------------------------ |
| v1.0 | 2026-05-27 | 初版；ID 命名、目录结构、工作流、YAML 头、现代都市视觉规范、AI 衔接、道具协议、有声段落 |
