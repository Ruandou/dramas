# 《超雄重生1995》制片规范 · ID 与引用

> **正名**：《超雄重生1995》  
> **版本**：v1.0 · 2026-05-23

---

## 一、ID 命名规则

| 类型     | 格式                              | 示例              | 用途                             |
| -------- | --------------------------------- | ----------------- | -------------------------------- |
| **角色** | `CHAR-###`                        | `CHAR-001`        | 对白、关系、选角（谁）           |
| **形象** | `CHAR-###-L##`                    | `CHAR-001-L01`    | 出图、妆造、分镜视觉（长什么样） |
| 群演     | `CHAR-GRP-##` / `CHAR-GRP-##-L01` | `CHAR-GRP-01-L01` | 通用脸型一套即可                 |
| 场景     | `SCENE-###`                       | `SCENE-002`       | 布景                             |
| 分集     | `EP##`                            | `EP01`            | 分集文件                         |
| 道具     | `PROP-###`                        | `PROP-001`        | 警徽、手铐、传呼机等             |

- 编号**只增不删**；弃用标 `deprecated: true`。
- 新建角色：先 `资产/角色索引.md`，再建 **`L01` 基础形象**于 `资产/形象索引.md` + `角色卡.md`。
- 新建换装/阶段：**只加 `L02+` 衍生形象**，并写明 `based_on: CHAR-xxx-L01`。

### 1.1 形象 ID 与基础 / 衍生

| 后缀     | 名称     | 要求                                                                                |
| -------- | -------- | ----------------------------------------------------------------------------------- |
| **L01**  | 基础形象 | 完整定妆 Prompt（脸、体型、发式、默认服装）；全剧面部一致性的**唯一锚点**           |
| **L02+** | 衍生形象 | **必须** `based_on` 本角色 `L01`；Prompt 只写差异，并含 `same face as CHAR-xxx-L01` |

**出图流程**：定稿 `L01` 参考图 → 衍生形象用图生图 / 角色一致性模型 → 禁止跳过 L01 直接生成 L02。

**本剧常见衍生场景**：

| 角色   | L01（基础）     | L02+（衍生示例）                  |
| ------ | --------------- | --------------------------------- |
| 陆峥   | 日常便装/警服   | L02 警校制服、L03 冻伤病号服      |
| 秦飒   | 刑侦队工作装   | L02 便衣外勤、L03 正式表彰礼服   |
| 林栀   | 白大褂急诊装   | L02 生活便装、L03 病房病号服      |

---

## 二、目录结构

```
超雄重生1995/
├── 资产/
│   ├── 角色索引.md      # CHAR-* 主表（角色卡详情可链入）
│   ├── 场景卡片.md      # SCENE-* 主表
│   └── 年代美术规范.md  # 1995年服饰/场景/道具/negative（必读）
├── 剧本/
│   ├── README.md
│   ├── _模板.md
│   ├── EP01_醒来.md
│   └── …
├── assets/
│   ├── generated/       # AI 生成成品（禁止写入占位）
│   │   └── EP##/
│   ├── looks/           # 角色定妆参考图
│   ├── scenes/          # 场景参考图
│   ├── props/           # 道具参考图
│   └── voices/          # 音色参考
├── docs/
│   └── Seedance2_有声段落流水线.md
├── script/              # 自动化脚本
├── 角色卡.md            # 外貌/Prompt 详情（含 CHAR ID）
├── 制片规范.md          # 本文件
├── 短剧剧本_超雄重生1995_36集.md
└── 人物小传_超雄重生1995.md
```

**约定**：

- **纲要**仍可在 `短剧剧本_超雄重生1995_36集.md` 维护剧情。
- **可拍分镜 + 对白**一律在 `剧本/EP##_*.md`，并带 CHAR/SCENE 引用。

---

## 三、工作流顺序（必读 · 防不同步）

### 3.1 修改顺序

改台词、场次、字幕/旁白策略时**严格按层向下**，**禁止**先改 `segments.yaml` 或 `shots.yaml` 再回头补剧本。

| 顺序  | 层           | 文件                                                                   | 写什么                                                                               |
| :---: | ------------ | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| **1** | **主剧本**   | [`短剧剧本_超雄重生1995_36集.md`](./短剧剧本_超雄重生1995_36集.md)     | 集纲、每场剧情、对白要点、钩子                                                       |
| **2** | **分集剧本** | [`剧本/EP##_*.md`](./剧本/)                                            | 可拍分镜表：`shot_id`、画面、**对白/备注**、CHAR/SCENE/形象、时长/模式/运镜          |
| **3** | **机器可读** | 同目录 `EP##_shots.yaml`                                               | 从分集 md 导出，勿手改 yaml 当源                                                     |
| **4** | **段落 API** | 同目录 `EP##_segments.yaml`                                            | 按 md 镜号合并为 segment；`api.text` 对白/字幕/旁白与 **第 2 步引号内字面一致**      |
| **5** | **其它**     | 声音卡、年代规范、assets/                                              | 仅在前几步定稿后随需更新                                                             |

**Agent / 人工自检**（改完第 2 步后）：

- [ ] 主剧本该集对白要点已与分集表一致（或注明「分集为准」并只改分集）
- [ ] 未单独改 `segments` 而 md 未动

**单集真相源**：拍什么、说什么 → **`剧本/EP##_*.md`**；主剧本为纲，segments 为 API 展开，**不是**编剧起点。

---

## 四、分集文件头（YAML）

每集文件顶部：

```yaml
---
episode_id: EP01
episode_title: 醒来
duration_min: 3
season: S1
scene_ids: [SCENE-001, SCENE-002, SCENE-003]
character_ids: [CHAR-001, CHAR-002, CHAR-GRP-01]
look_ids: [CHAR-001-L01, CHAR-002-L01]
seedance_defaults:
  model: seedance-2.0
  ratio: "9:16"
  resolution: 720p
  duration_sec: 10-12
  generate_audio: true
---
```

**字段说明**：

| 字段            | 说明                                    |
| --------------- | --------------------------------------- |
| `episode_id`    | `EP##` 格式                             |
| `episode_title` | 集标题                                  |
| `duration_min`  | 目标时长（分钟），本剧 2–3 分钟         |
| `season`        | 固定 `S1`（全 36 集为第一季）           |
| `scene_ids`     | 本集涉及场景 ID 列表                    |
| `character_ids` | 本集涉及角色 ID 列表                    |
| `look_ids`      | 本集涉及形象 ID 列表                    |
| `seedance_defaults` | Seedance 生成默认参数               |

---

## 五、1995 年代视觉约束

### 5.1 时代特征（必须体现）

| 类别         | 准确元素                                                                         |
| ------------ | -------------------------------------------------------------------------------- |
| **通讯设备** | BP 机（传呼机）、公用电话亭、IC 卡电话、座机（转盘/按键）、少量大哥大（砖头机） |
| **家电**     | CRT 电视机（14–21 寸）、VCD 机、录像带、收录机、磁带                            |
| **交通**     | 桑塔纳、212 吉普、自行车（飞鸽/永久）、绿皮公交、长途客运大巴                   |
| **建筑**     | 筒子楼、单位宿舍、水泥砖墙、铁门铁窗、老式派出所（木桌铁柜）                   |
| **服饰**     | 夹克衫、中山装残余、西装（宽肩垫肩）、涤纶衬衫、的确良、碎花裙                 |
| **公安制式** | 83 式/89 式警服、大盖帽、老式手铐、五四手枪（木质枪套）                          |
| **商业环境** | 个体户铁皮棚、菜市场水泥台、国营百货柜台、「下岗再就业」标语                    |
| **文娱**     | 录像厅、台球室、街边象棋摊、报刊亭                                              |

### 5.2 禁止出现（Negative Prompt 必加）

| 类别           | 禁止元素                                                                                |
| -------------- | --------------------------------------------------------------------------------------- |
| **通讯**       | 智能手机、翻盖/滑盖手机（1998年后）、笔记本电脑、平板                                  |
| **家电**       | 液晶/LED 平板电视、蓝牙音箱、智能家居                                                  |
| **交通**       | 2000 年后车型、SUV、电动车/电动摩托、高铁、地铁（1995年本市无）                         |
| **建筑**       | 玻璃幕墙高楼、LED 广告屏、现代商场                                                     |
| **服饰**       | 运动品牌 Logo（Nike/Adidas 当年不普及）、韩系穿搭、牛仔破洞装                          |
| **道具**       | 二维码、快递包裹、外卖箱、充电宝、一次性塑料奶茶杯                                    |
| **场景氛围**   | 霓虹 LED 灯带、RGB 光效、现代极简装修                                                  |

### 5.3 Negative Prompt 模板

```
smartphone, flat screen TV, LED lights, modern car, laptop, tablet,
QR code, delivery box, power bank, glass curtain wall, subway station,
LED billboard, Nike logo, Adidas logo, wireless earbuds, ring light,
bubble tea cup, modern minimalist interior, neon RGB lighting
```

### 5.4 车辆与环境参考

| 场景类型     | 正确车辆/环境                                          |
| ------------ | ------------------------------------------------------ |
| 派出所门口   | 212 吉普、自行车排、老式摩托车                         |
| 街道         | 桑塔纳出租车（红色/黄色）、三轮车、板车               |
| 长途客运站   | 大巴（圆头/方头老式）、微型面包车                     |
| 医院         | 白色方形老式救护车、三轮推车                           |
| 棉纺厂       | 老式卡车（东风/解放）、厂区自行车棚                   |

---

## 六、AI 出图/视频衔接

### 6.1 出图流程

1. 分镜 **`场景`** → `资产/场景卡片.md` → `SCENE-###`
2. 分镜 **`形象`** → `资产/形象索引.md` → `角色卡.md` 内该 **L##** 的 Prompt
3. **L01**：用「基础 Prompt」生成 Character Sheet（正面全身，白底）
4. **L02+**：`based_on` 指向 L01 参考图 + **delta Prompt**（仅服装/道具/妆效/伤痕）
5. 多角色同框：按形象 ID 顺序合并；每人脸对应其 `L##`，禁止混用别角 L01
6. 分镜 **`角色`** 列仅用于对白表，不参与出图 Prompt 拼接

### 6.2 角色一致性规则

- 每个角色 **L01** 生成后，所有后续出现必须使用该 L01 作为 reference_image
- 衍生形象仅改变服装/状态，面部特征锁定为 L01
- 多人同框时，每人必须挂对应的 `looks/CHAR-*-L##.png`

### 6.3 年代合规 Negative Prompt

所有生成任务（图片/视频）**必须**在 negative prompt 中包含 §5.3 模板内容，确保无现代穿帮。

### 6.4 场景出图要求

- 每个 `SCENE-*` 必须有对应参考图 `assets/scenes/SCENE-*.png`
- 场景参考图必须符合 §5.1 时代特征
- 室内场景注意：老式日光灯管、水泥/石灰墙面、木质/铁皮桌柜

### 6.5 引用图片提交策略（TOS 优先）

Seedance 2.0 API 文档标称支持每段最多 9 张引用图片，但使用 base64 data URI 时，4+ 张高分辨率图片（2K PNG，单张 200-500KB）会因请求体积过大导致提交失败。

**策略：TOS 优先，base64 兜底**

**两级引用策略（优先级从高到低）**：

| 优先级 | 方式 | 有效期 | 适用场景 |
|--------|------|--------|----------|
| 1 | TOS URL | 永久 | 已上传至 TOS 的图片，请求体积最小 |
| 2 | base64 data URI | 永久 | TOS 未上传时的兜底方案，≤3 张/段 |

**TOS 永久图床**：
- 桶名/区域默认：`drama-reference-images` / `cn-beijing`
- URL 格式：`https://{bucket}.tos-cn-beijing.volces.com/{prefix}/{filename}`
- 上传工具：`mcps/volc-ark/scripts/tos_upload.py sync --project-root .`
- 上传后在 `cdn_urls.json` 的 `tos_url` 字段存储永久链接（`tos_url` 为首选字段）

**实施要求**：
- 上传图片至 TOS 后，在 `assets/looks/` 或 `assets/scenes/` 同目录下维护 `cdn_urls.json`：
  ```json
  {
    "CHAR-001-L01": {
      "local": "CHAR-001-L01.png",
      "tos_url": "https://drama-reference-images.tos-cn-beijing.volces.com/looks/CHAR-001-L01.png"
    }
  }
  ```
- 提交脚本优先读取 `tos_url`；若无则读取本地文件转 base64
- **TOS 模式下**：允许 4-6 张引用图片/段（多角色场景不再受限）
- **base64 模式下**：保守限制 ≤3 张引用图片/段
- 本地 PNG 文件始终保留作为最终备份

---

## 七、道具转移与肢体动作（防错清单）

适用：缴枪、递证件、追逐格斗等 **「谁碰什么」** 明确的镜。

### 7.1 常见模型错误

| 现象           | 原因                                                                       |
| -------------- | -------------------------------------------------------------------------- |
| 道具尺度漂移   | 手枪忽大忽小；prompt 写「枪」无具体描述                                    |
| 配角先动手     | 上一镜配角对白，下一句「掏枪/制服」未绑 **图1 主角**；多人同框动作归属模糊 |
| 段内跳切不连贯 | prompt 写成镜头清单，各镜各画，不保证同一把道具、同一人                    |
| 年代穿帮       | 生成的枪械/手铐为现代款式                                                  |

### 7.2 分段原则（先于 prompt）

- **同一场戏、同一道具动作链** → **一条 segment**，勿拆成多条 API 任务。
- 对白镜与动作镜在同一 segment 时：**对白者 ≠ 动作者** 必须写清绑定。
- 单段 **≤1 句对白**；复杂肢体段尽量少人同框。

### 7.3 `api.text` 写法（强制）

1. **段首「角色分工」**：谁可碰道具、谁 **禁止**（例：仅图1 陆峥可持枪；图2 嫌疑人双手不得拿枪）。
2. **每镜绑 `图N` + 服装色**：用 `图1`/`图2`，不用泛称「男主」「嫌犯」；`content_roles` 顺序与全文一致。
3. **道具锁定**：具体描述（五四手枪，木质枪套，标准尺寸）；禁止堆叠模糊修饰词。
4. **因果链叙事**，非 Excel 清单：  
   `图2 逃跑 → 图1 追击 → 图1 掏枪警告 → 图2 停步举手`
5. **关键镜重复约束**：格斗、缴枪镜再写一次「图1 持枪 / 图2 空手」。

### 7.4 参考图

| 资产 | 路径                          | 用途               |
| ---- | ----------------------------- | ------------------ |
| 定妆 | `assets/looks/CHAR-*-L01.png` | 脸与服装           |
| 场景 | `assets/scenes/SCENE-*.png`   | 空间               |
| 道具 | `assets/props/PROP-*-*.png`   | 尺度与造型锚点     |

### 7.5 提交前检查

- [ ] 段首是否有 **角色分工 + 对手禁止碰道具**？
- [ ] 每句「掏枪/制服/递交」是否带 **图N**？
- [ ] 道具动作链是否 **未拆段**？`duration_sec` 是否 4–12？
- [ ] `content_roles` 图1 是否为 **剧本上的动作者**？
- [ ] 有 `PROP-*` 时是否已出图并挂入 `content_roles`？
- [ ] Negative prompt 是否包含 §5.3 年代禁止项？

### 7.6 `segments.yaml` 模板片段（追捕类）

```yaml
api:
  text: |
    【图1】陆峥·男主（89式警服，大盖帽）【图2】嫌疑人（灰色夹克，平头）【图3】SCENE-005
    角色分工：仅图1可持枪；图2禁止持有任何武器。
    道具：五四手枪，木质枪套，标准尺寸，禁止变大。
    图2在长途客运站人群中逃跑；图1追击，从腰间枪套拔枪。
    图1持枪高举警告「站住！警察！」；图2 停步双手举过头顶。
    图1上前用老式手铐铐住图2 双手。
content_roles:
  - { file: CHAR-001-L01, role: reference_image, label: 图1 }
  - { file: CHAR-GRP-02-L01, role: reference_image, label: 图2 }
  - { file: SCENE-005, role: reference_image, label: 图3 }
```

---

## 八、有声段落（Seedance 2.0 · 默认）

1. **提交单位**：`EP##_segments.yaml` 中每条 `segment_id`（约 10–12 秒），非一镜一任务。
2. **音频**：`generate_audio: true`；对白与 **voice_prompt** 写在 `api.text`。
3. **画面**：段落仅用 `looks/` + `scenes/`；不依赖镜级首帧。
4. **一段一落音**：单段主说话人 ≤2；长对手戏拆成 `SEGxxa` / `SEGxxb`。
5. **音色文案**：同一角色跨段 **voice_prompt 全文一致**。  
   - 陆峥：「成年男性，25岁，沉稳低沉，语速偏慢，有阅历感」  
   - 秦飒：「成年女性，28岁，冷静干练，语速中等，不带感情色彩」  
   5.1 **字幕（默认全段）**：Seedance 画面出简体白字底字幕，与对白同步。  
   5.2 **字卡镜**：并入相邻段，画面字幕一字不差，无旁白念稿。
6. **验收**：全段耳听+目视字幕是否与引号一致。
7. **成片**：`assets/generated/EP##/` 下 `EP##-SEG*.mp4`；集级 `script/ffmpeg_concat_episode.sh`。
8. **段落时长（API）**：`duration_sec` 对 **fast** 只能 **4–12**（硬限制）。
9. **合理分段（制片）**：
   - **一条 API = 一段可拍的戏（约 4–12 秒信息密度）**，不是「一镜一条」。
   - 分镜表里某镜自然时长 **< 4 秒**：**并入同场景相邻段**，不要单独开段。
   - 拆段边界：场切换、主说话人切换、情绪断点；不要按 `shot_id` 机械切。

### 8.10 Seedance 段落 Prompt 结构规范

为避免双字幕和字幕不一致，每段 `api.text` 必须遵循以下结构：

```
【图1】角色 ID【图2】场景 ID。
竖屏9比16连贯叙事。
镜头1（Xs）[景别][运镜]：[纯视觉动作描述，不含对白]
镜头2（Xs）[景别][运镜]：[纯视觉动作描述，不含对白]
对白（角色A，年龄）：「台词」
对白（角色B，年龄）：「台词」
[negative_prompt]
[prompt_suffix - 含统一字幕指令]
```

**禁止事项：**
1. **禁止**在镜头描述中嵌入对白文字（会导致双字幕）
2. **禁止**在段落级添加 `字幕：...` 指令（全局 `prompt_suffix` 已包含统一字幕规则）
3. **禁止**在多处重复出现相同对白文字

**字幕统一由 `prompt_suffix` 控制：**
`"对白时画面底部居中简体白字字幕与语音同步；禁止其它乱字"`

该规则确保：
- 对白集中在一处，Seedance 仅渲染一次字幕
- 字幕样式由全局指令统一控制
- 视觉描述与对白分离，减少模型混淆

---

## 九、修订记录

| 版本 | 日期       | 说明                                                   |
| ---- | ---------- | ------------------------------------------------------ |
| v1.0 | 2026-05-23 | 初版；ID 命名、目录结构、工作流、YAML 头、年代约束、AI 衔接、道具协议 |
